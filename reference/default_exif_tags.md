# Default EXIF, XMP-dc, and IPTC tags

A vector of default EXIF, XMP-dc, and IPTC tags (including wildcard EXIF
tag "GPS") used by
[`read_exif()`](https://elipousson.github.io/filenamr/reference/read_exif.md).

## Usage

``` r
default_exif_tags
```

## Format

A length 26 character vector.
